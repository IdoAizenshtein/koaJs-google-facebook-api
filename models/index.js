const {Sequelize} = require('sequelize');require('dotenv').config();let {PGHOST, PGDATABASE, PGUSER, PGPASSWORD, ENDPOINT_ID} = process.env;// const settings = {//     // host: '34.165.205.247',//     dialect: 'postgres',//     pool: {//         max: 5,//         min: 0,//         acquire: 30000,//         idle: 10000,//     },//     host: process.env.INSTANCE_UNIX_SOCKET,//     timestamps: false,//     dialectOptions: {//         socketPath: process.env.INSTANCE_UNIX_SOCKET//     }// }// const sequelize = new Sequelize('postgres', 'postgres', 'Gemini2024', settings);// console.log(PGHOST, PGDATABASE, PGUSER, PGPASSWORD, ENDPOINT_ID)const sequelize = new Sequelize(PGDATABASE, PGUSER, PGPASSWORD, {    dialect: 'postgres',    ssl: true,    host: PGHOST,    logging: false,    dialectOptions: {        project: ENDPOINT_ID,        ssl: {            require: true,            rejectUnauthorized: false,        },    },    pool: {        max: 5,        min: 0,        acquire: 30000,        idle: 20000    }});try {    sequelize.authenticate().then(r => {        console.log("Connection has been established successfully.");        // sequelize.sync({force: true}).then(r =>{        //     console.log("Connection has sync");        // });    });} catch (err) {    console.error("Unable to connect to the database:", err);}module.exports = sequelize;